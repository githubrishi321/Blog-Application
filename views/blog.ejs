<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('./partials/head') %>
    <title>
      <%= blog.title %> - Blogify
    </title>
    <style>
      /* Blog Page Specific Styles */
      .blog-hero {
        background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(139, 92, 246, 0.1));
        border-radius: 20px;
        padding: 40px;
        margin-bottom: 30px;
        position: relative;
        overflow: hidden;
      }

      .blog-hero::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 100%;
        height: 200%;
        background: radial-gradient(circle, rgba(99, 102, 241, 0.1) 0%, transparent 70%);
        animation: pulse 4s ease-in-out infinite;
      }

      @keyframes pulse {

        0%,
        100% {
          opacity: 0.5;
          transform: scale(1);
        }

        50% {
          opacity: 0.8;
          transform: scale(1.1);
        }
      }

      .blog-category {
        display: inline-block;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        color: white;
        padding: 6px 16px;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        margin-bottom: 15px;
      }

      .blog-metadata {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-top: 20px;
        padding-top: 20px;
        border-top: 1px solid rgba(99, 102, 241, 0.2);
      }

      .metadata-item {
        display: flex;
        align-items: center;
        gap: 8px;
        color: var(--gray-color);
        font-size: 0.95rem;
      }

      .metadata-item i {
        color: var(--primary-color);
      }

      .blog-cover-container {
        position: relative;
        border-radius: 20px;
        overflow: hidden;
        margin-bottom: 30px;
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      }

      .blog-cover-container img {
        width: 100%;
        max-height: 500px;
        object-fit: cover;
        transition: transform 0.5s ease;
      }

      .blog-cover-container:hover img {
        transform: scale(1.02);
      }

      .blog-cover-overlay {
        position: absolute;
        bottom: 0;
        left: 0;
        right: 0;
        background: linear-gradient(transparent, rgba(0, 0, 0, 0.7));
        padding: 40px 30px 20px;
      }

      .blog-body {
        font-size: 1.15rem;
        line-height: 1.9;
        color: #374151;
        background: white;
        border-radius: 20px;
        padding: 40px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        margin-bottom: 30px;
      }

      .blog-body p {
        margin-bottom: 1.5rem;
      }

      .author-card {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border-radius: 20px;
        padding: 30px;
        color: white;
        margin-bottom: 30px;
        position: relative;
        overflow: hidden;
      }

      .author-card::before {
        content: '';
        position: absolute;
        top: 0;
        right: 0;
        width: 150px;
        height: 150px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 50%;
        transform: translate(30%, -30%);
      }

      .author-card-avatar {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        object-fit: cover;
        border: 4px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      }

      .author-card-name {
        font-size: 1.5rem;
        font-weight: 700;
        margin-bottom: 5px;
      }

      .author-card-label {
        opacity: 0.9;
        font-size: 0.95rem;
      }

      .breadcrumb-nav {
        background: rgba(255, 255, 255, 0.9);
        backdrop-filter: blur(10px);
        border-radius: 12px;
        padding: 12px 20px;
        margin-bottom: 20px;
        display: inline-flex;
        align-items: center;
        gap: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
      }

      .breadcrumb-nav a {
        color: var(--primary-color);
        text-decoration: none;
        font-weight: 500;
        transition: all 0.3s ease;
      }

      .breadcrumb-nav a:hover {
        color: var(--secondary-color);
      }

      .breadcrumb-nav span {
        color: var(--gray-color);
      }

      .comments-header {
        background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(139, 92, 246, 0.1));
        border-radius: 15px;
        padding: 20px 25px;
        margin-bottom: 25px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 15px;
      }

      .comments-count {
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        color: white;
        padding: 8px 16px;
        border-radius: 20px;
        font-weight: 600;
        font-size: 0.9rem;
      }

      .comment-form-card {
        background: linear-gradient(135deg, #f8fafc, #e2e8f0);
        border-radius: 20px;
        padding: 25px;
        margin-bottom: 30px;
        border: 2px solid transparent;
        transition: all 0.3s ease;
      }

      .comment-form-card:focus-within {
        border-color: var(--primary-color);
        box-shadow: 0 10px 30px rgba(99, 102, 241, 0.15);
      }

      .comment-item {
        background: white;
        border-radius: 16px;
        padding: 20px;
        margin-bottom: 15px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        border-left: 4px solid transparent;
        transition: all 0.3s ease;
      }

      .comment-item:hover {
        border-left-color: var(--primary-color);
        transform: translateX(5px);
      }

      .empty-comments {
        text-align: center;
        padding: 50px 20px;
        background: linear-gradient(135deg, #f8fafc, #e2e8f0);
        border-radius: 20px;
      }

      .empty-comments i {
        font-size: 4rem;
        background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 20px;
        display: block;
      }

      .share-buttons {
        display: flex;
        gap: 10px;
        flex-wrap: wrap;
      }

      .share-btn {
        width: 45px;
        height: 45px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        text-decoration: none;
        transition: all 0.3s ease;
        font-size: 1.1rem;
      }

      .share-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        color: white;
      }

      .share-btn.twitter {
        background: #1DA1F2;
      }

      .share-btn.facebook {
        background: #4267B2;
      }

      .share-btn.linkedin {
        background: #0077B5;
      }

      .share-btn.whatsapp {
        background: #25D366;
      }

      .sidebar-card {
        background: white;
        border-radius: 20px;
        padding: 25px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
        margin-bottom: 20px;
      }

      .sidebar-title {
        font-weight: 700;
        font-size: 1.1rem;
        color: var(--dark-color);
        margin-bottom: 15px;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      .sidebar-title i {
        color: var(--primary-color);
      }

      @media (max-width: 768px) {
        .blog-hero {
          padding: 25px;
        }

        .blog-body {
          padding: 25px;
          font-size: 1.05rem;
        }

        .author-card {
          padding: 20px;
        }

        .author-card-avatar {
          width: 60px;
          height: 60px;
        }

        .author-card-name {
          font-size: 1.25rem;
        }

        .blog-cover-container img {
          max-height: 300px;
        }

        .comments-header {
          padding: 15px 20px;
        }

        .share-btn {
          width: 40px;
          height: 40px;
        }
      }

      @media (max-width: 576px) {
        .blog-hero {
          padding: 20px;
        }

        .blog-metadata {
          gap: 15px;
        }

        .blog-body {
          padding: 20px;
          font-size: 1rem;
        }
      }
    </style>
</head>

<body>
  <%- include('./partials/nav') %>

    <div class="container">
      <div class="main-content">
        <!-- Breadcrumb Navigation -->
        <div class="breadcrumb-nav">
          <a href="/"><i class="fas fa-home"></i> Home</a>
          <span>/</span>
          <span class="text-truncate" style="max-width: 200px;">
            <%= blog.title %>
          </span>
        </div>

        <div class="row">
          <!-- Main Content Column -->
          <div class="col-lg-8">
            <!-- Blog Hero Section -->
            <div class="blog-hero">
              <span class="blog-category">
                <i class="fas fa-bookmark me-2"></i>Blog Post
              </span>
              <h1 class="blog-title mb-3">
                <%= blog.title %>
              </h1>

              <div class="blog-metadata">
                <% if (blog.createdBy) { %>
                  <div class="metadata-item">
                    <img src="<%= blog.createdBy.profileImageURL %>" class="rounded-circle" width="28" height="28"
                      style="object-fit: cover;" onerror="this.src='/Images/default.svg';" />
                    <span>
                      <%= blog.createdBy.fullName %>
                    </span>
                  </div>
                  <% } %>
                    <div class="metadata-item">
                      <i class="fas fa-calendar-alt"></i>
                      <span>
                        <%= new Date(blog.createdAt).toLocaleDateString('en-US', { year: 'numeric' , month: 'long' ,
                          day: 'numeric' }) %>
                      </span>
                    </div>
                    <div class="metadata-item">
                      <i class="fas fa-clock"></i>
                      <span>
                        <%= Math.ceil(blog.body.length / 1000) %> min read
                      </span>
                    </div>
              </div>
            </div>

            <!-- Blog Cover Image -->
            <% if (blog.coverImageURL) { %>
              <div class="blog-cover-container">
                <img src="<%= blog.coverImageURL %>" alt="<%= blog.title %>"
                  onerror="this.parentElement.style.display='none';" />
              </div>
              <% } %>

                <!-- Blog Content -->
                <div class="blog-body">
                  <% // Split content into paragraphs for better formatting const
                    paragraphs=blog.body.split('\n').filter(p=> p.trim());
                    %>
                    <% paragraphs.forEach(paragraph=> { %>
                      <p>
                        <%= paragraph %>
                      </p>
                      <% }) %>
                </div>

                <!-- Share Buttons -->
                <div class="sidebar-card">
                  <div class="sidebar-title">
                    <i class="fas fa-share-alt"></i>
                    Share this article
                  </div>
                  <div class="share-buttons">
                    <a href="https://twitter.com/intent/tweet?text=<%= encodeURIComponent(blog.title) %>"
                      target="_blank" class="share-btn twitter" title="Share on Twitter">
                      <i class="fab fa-twitter"></i>
                    </a>
                    <a href="https://www.facebook.com/sharer/sharer.php?quote=<%= encodeURIComponent(blog.title) %>"
                      target="_blank" class="share-btn facebook" title="Share on Facebook">
                      <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="https://www.linkedin.com/shareArticle?mini=true&title=<%= encodeURIComponent(blog.title) %>"
                      target="_blank" class="share-btn linkedin" title="Share on LinkedIn">
                      <i class="fab fa-linkedin-in"></i>
                    </a>
                    <a href="https://wa.me/?text=<%= encodeURIComponent(blog.title) %>" target="_blank"
                      class="share-btn whatsapp" title="Share on WhatsApp">
                      <i class="fab fa-whatsapp"></i>
                    </a>
                  </div>
                </div>

                <!-- Comments Section -->
                <div class="comment-section">
                  <div class="comments-header">
                    <h3 class="mb-0" style="font-weight: 700;">
                      <i class="fas fa-comments me-2" style="color: var(--primary-color);"></i>
                      Discussion
                    </h3>
                    <span class="comments-count">
                      <i class="fas fa-message me-1"></i>
                      <%= comments.length %>
                        <%= comments.length===1 ? 'Comment' : 'Comments' %>
                    </span>
                  </div>

                  <!-- Add Comment Form -->
                  <% if (locals.user) { %>
                    <div class="comment-form-card">
                      <div class="d-flex align-items-center gap-3 mb-3">
                        <img src="<%= locals.user.profileImageURL %>" class="rounded-circle" width="45" height="45"
                          style="object-fit: cover;" onerror="this.src='/Images/default.svg';" />
                        <div>
                          <h6 class="mb-0 fw-bold">
                            <%= locals.user.fullName %>
                          </h6>
                          <small class="text-muted">Share your thoughts</small>
                        </div>
                      </div>
                      <form action="/blog/comment/<%= blog._id %>" method="post">
                        <div class="mb-3">
                          <textarea name="content" class="form-control"
                            placeholder="What do you think about this article?" rows="4" required
                            style="resize: none;"></textarea>
                        </div>
                        <button class="btn btn-primary" type="submit">
                          <i class="fas fa-paper-plane me-2"></i>Post Comment
                        </button>
                      </form>
                    </div>
                    <% } else { %>
                      <div class="alert alert-info d-flex align-items-center" style="border-radius: 15px;">
                        <i class="fas fa-info-circle fa-lg me-3"></i>
                        <div>
                          <strong>Join the conversation!</strong><br>
                          <a href="/user/signin" class="alert-link">Sign in</a> to share your thoughts and engage with
                          others.
                        </div>
                      </div>
                      <% } %>

                        <!-- Comments List -->
                        <% if (comments.length===0) { %>
                          <div class="empty-comments">
                            <i class="fas fa-comments"></i>
                            <h4 class="fw-bold mb-2">No comments yet</h4>
                            <p class="text-muted mb-0">Be the first to share your thoughts on this article!</p>
                          </div>
                          <% } else { %>
                            <% comments.forEach((comment, index)=> { %>
                              <div class="comment-item" style="animation: fadeIn 0.5s ease <%= index * 0.1 %>s both;">
                                <div class="d-flex align-items-start gap-3">
                                  <% if (comment.createdBy) { %>
                                    <img src="<%= comment.createdBy.profileImageURL %>"
                                      class="rounded-circle flex-shrink-0" width="45" height="45"
                                      style="object-fit: cover;" onerror="this.src='/Images/default.svg';" />
                                    <div class="flex-grow-1">
                                      <div
                                        class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-2">
                                        <h6 class="mb-0 fw-bold">
                                          <%= comment.createdBy.fullName %>
                                        </h6>
                                        <small class="text-muted">
                                          <i class="fas fa-clock me-1"></i>
                                          <%= new Date(comment.createdAt).toLocaleDateString('en-US', { year: 'numeric'
                                            , month: 'short' , day: 'numeric' }) %>
                                        </small>
                                      </div>
                                      <p class="mb-0" style="color: var(--dark-color); line-height: 1.7;">
                                        <%= comment.content %>
                                      </p>
                                    </div>
                                    <% } else { %>
                                      <img src="/Images/default.svg" class="rounded-circle flex-shrink-0" width="45"
                                        height="45" />
                                      <div class="flex-grow-1">
                                        <div
                                          class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-2">
                                          <h6 class="mb-0 fw-bold text-muted">Unknown User</h6>
                                          <small class="text-muted">
                                            <i class="fas fa-clock me-1"></i>
                                            User information not available
                                          </small>
                                        </div>
                                        <p class="mb-0" style="color: var(--dark-color); line-height: 1.7;">
                                          <%= comment.content %>
                                        </p>
                                      </div>
                                      <% } %>
                                </div>
                              </div>
                              <% }) %>
                                <% } %>
                </div>
          </div>

          <!-- Sidebar Column -->
          <div class="col-lg-4">
            <!-- Author Card -->
            <div class="author-card">
              <div class="d-flex align-items-center gap-3">
                <% if (blog.createdBy) { %>
                  <img src="<%= blog.createdBy.profileImageURL %>" class="author-card-avatar"
                    onerror="this.src='/Images/default.svg';" />
                  <div>
                    <h4 class="author-card-name">
                      <%= blog.createdBy.fullName %>
                    </h4>
                    <p class="author-card-label mb-0">
                      <i class="fas fa-pen me-2"></i>Author
                    </p>
                  </div>
                  <% } else { %>
                    <img src="/Images/default.svg" class="author-card-avatar" />
                    <div>
                      <h4 class="author-card-name">Unknown Author</h4>
                      <p class="author-card-label mb-0">Author information not available</p>
                    </div>
                    <% } %>
              </div>
            </div>

            <!-- Quick Actions -->
            <div class="sidebar-card">
              <div class="sidebar-title">
                <i class="fas fa-bolt"></i>
                Quick Actions
              </div>
              <div class="d-grid gap-2">
                <a href="/" class="btn btn-outline-primary">
                  <i class="fas fa-arrow-left me-2"></i>Back to Home
                </a>
                <% if (locals.user) { %>
                  <a href="/blog/add-new" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Write a Blog
                  </a>
                  <% } %>
              </div>
            </div>

            <!-- Article Info -->
            <div class="sidebar-card">
              <div class="sidebar-title">
                <i class="fas fa-info-circle"></i>
                Article Info
              </div>
              <ul class="list-unstyled mb-0">
                <li class="d-flex align-items-center gap-3 mb-3">
                  <i class="fas fa-calendar text-primary"></i>
                  <div>
                    <small class="text-muted d-block">Published</small>
                    <span>
                      <%= new Date(blog.createdAt).toLocaleDateString('en-US', { year: 'numeric' , month: 'long' ,
                        day: 'numeric' }) %>
                    </span>
                  </div>
                </li>
                <li class="d-flex align-items-center gap-3 mb-3">
                  <i class="fas fa-clock text-primary"></i>
                  <div>
                    <small class="text-muted d-block">Reading Time</small>
                    <span>
                      <%= Math.ceil(blog.body.length / 1000) %> min read
                    </span>
                  </div>
                </li>
                <li class="d-flex align-items-center gap-3">
                  <i class="fas fa-comments text-primary"></i>
                  <div>
                    <small class="text-muted d-block">Comments</small>
                    <span>
                      <%= comments.length %>
                        <%= comments.length===1 ? 'comment' : 'comments' %>
                    </span>
                  </div>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%- include('./partials/footer') %>
      <%- include('./partials/scripts') %>

        <style>
          @keyframes fadeIn {
            from {
              opacity: 0;
              transform: translateY(10px);
            }

            to {
              opacity: 1;
              transform: translateY(0);
            }
          }
        </style>
</body>

</html>
</CodeContent>
<parameter name="EmptyFile">false